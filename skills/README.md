# Skills

Add each skill as a directory directly under this directory. A valid skill has this shape:

```text
skills/
└── skill-name/
    └── SKILL.md
```

Do not add placeholder `SKILL.md` files here; any valid file may be detected and published by Agent Skills tooling.
